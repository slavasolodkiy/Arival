You are continuing the existing repo: slavasolodkiy/Arival.
Goal: convert current demo into a reliable v2 foundation with real onboarding state machine + mobile foundation.

Hard constraints:
1) Keep project legal and original (no trademark/code copy from Arival).
2) Treat provided legacy PDFs as historical context only, never as confirmed current truth.
3) Every architecture/integration claim must be tagged: source, as_of_date, confidence, confirmed_publicly.
4) Do not remove working features while refactoring.

Phase A — Critical correctness & security fixes
- Fix TypeScript build failure in lib/api-zod (duplicate exports).
- Implement strict onboarding state machine:
  - enforce currentStep validation
  - reject out-of-order steps
  - support branching safely
  - no auto-approval in normal mode
- Add explicit DEMO_MODE flag; keep auto-approve only when DEMO_MODE=true.
- Payments:
  - require OTP for confirm flow
  - validate OTP in backend (demo provider allowed but must verify code lifecycle)
- Replace sql.raw array interpolation with safe Drizzle patterns (no raw string array injection).
- Replace permissive CORS with allowlist via env var.

Phase B — True onboarding parity (web)
- Refactor web onboarding to be config-driven from onboarding JSON and backend step state.
- Implement full business flow UI (directors/UBO loop, conditional docs, authorized signatory).
- Remove hardcoded step overrides in frontend.
- Persist repeated director/UBO entries as arrays, not overwritten by step key.

Phase C — Mobile app foundation
- Create artifacts/nexvault-mobile (Expo React Native, TypeScript).
- Implement:
  - auth screens (register/login)
  - onboarding start/status/step screens (same backend API)
  - dashboard summary screen
- Shared API client generation must work for web + mobile.

Phase D — Evidence hygiene and docs integrity
- Add docs/research/evidence-matrix.md:
  columns = claim, source_url_or_file, source_type(public/legacy), as_of_date, confidence, confirmation_status.
- Update docs to distinguish:
  - implemented now
  - planned
  - inferred hypotheses
- Remove/adjust statements that currently over-claim “microservices”.

Phase E — Tests & CI
- Add tests for:
  - onboarding step-order enforcement
  - business branching paths
  - OTP transfer confirmation
- Add GitHub Actions:
  - pnpm install
  - typecheck
  - tests
- Provide coverage report summary.

Definition of done:
- `pnpm run typecheck` passes.
- Tests pass in CI.
- Web onboarding supports both individual and business end-to-end.
- Mobile app boots and completes onboarding happy-path against API.
- No critical security shortcuts active unless DEMO_MODE=true.
- Commit all changes and open/update PR with:
  - what changed
  - what is still mock
  - next risks
