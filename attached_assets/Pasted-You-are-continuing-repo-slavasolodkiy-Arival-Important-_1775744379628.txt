You are continuing repo slavasolodkiy/Arival.
Important constraint: DO NOT add `.github/workflows/ci.yml` (this repo intentionally avoids GitHub Actions).

Goal of this iteration: deep onboarding decomposition + integration readiness, with executable artifacts (not just prose).

1) Build machine-readable onboarding matrix
- Add `config/onboarding/catalog/` with:
  - `countries.json` (supported country codes + risk tier + availability flags)
  - `languages.json` (supported UI languages per country)
  - `identity-documents.json` (passport/ID/residence permit etc by country)
  - `individual-questions.json`
  - `business-questions.json`
  - `branch-rules.json` (if-answer-X -> next-step-Y rules)
- Keep format explicit and deterministic (stable IDs for every question/answer/branch).

2) Expose onboarding introspection APIs
- Add endpoints in api-server:
  - `GET /api/onboarding/catalog?flowType=individual|business&country=GB&language=en`
  - `POST /api/onboarding/preview` with payload `{ flowType, country, language, answersSoFar }`
- Response must include:
  - next question
  - allowed answers
  - branch reason
  - missing requirements
- Add OpenAPI entries + regenerate/patch generated clients/schemas accordingly.

3) Add coverage tests for branching logic
- Add route/integration tests to verify:
  - both flow types
  - at least 3 countries with different document sets
  - language fallback behavior
  - branch divergence when answers differ
- Keep tests deterministic and runnable via existing test command.

4) Add external integration map (ID/SSO/onboarding)
- Create `docs/external-integrations-map.md`:
  - table with provider/integration point/auth method/current status
  - mark each row as `Observed`, `Inferred`, or `Planned`
  - add evidence source per row (file path or public link)
- Explicitly include section:
  - “Public APIs for external integration (especially ID/SSO/onboarding): confirmed vs unknown”

5) Add local quality gate (instead of CI)
- Add `scripts/verify-local.ps1` and npm script `pnpm run verify:local` that runs:
  - typecheck
  - api-server tests
  - any schema sync checks you already have
- Update docs to instruct running this before every push from Replit.

Acceptance criteria:
- `pnpm run typecheck` passes
- `pnpm --filter @workspace/api-server test` passes
- New onboarding catalog APIs return structured branch-aware output
- OpenAPI and generated clients remain in sync
- No `.github/workflows/ci.yml` in repo

Output format:
- concise changelog
- exact commands run with pass/fail
- list of new files
- remaining gaps/risks
- final commit hash
